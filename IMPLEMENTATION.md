# Ringo / 링고 — 구현 현황과 운영 준비

> 2026-09-16 기준. 이전 버전(브라우저 localStorage 데모)을 서버 기반 실서비스 구조로 전면 전환했습니다.

## 1. 무엇이 바뀌었나

| 항목 | 이전 (Codex Sites 데모) | 현재 |
|---|---|---|
| 실행 | Vinext + Cloudflare Workers 미리보기 | 표준 Next.js 16 (`next dev/build/start`, `output: standalone`) |
| 데이터 | 브라우저 localStorage / IndexedDB | PostgreSQL (Drizzle ORM). 로컬은 설치 없는 PGlite, 운영은 AWS RDS/Aurora |
| 로그인 | 역할 선택 버튼 (보안 없음) | 이메일+비밀번호(scrypt), DB 세션, 서버 권한 검사, 비밀번호 재설정, 이메일 인증 |
| 파일 | 브라우저 저장 (25MB) | 로컬 디스크 또는 S3 비공개 저장, 구매 권한 확인 후 다운로드 |
| 결제 | 가짜 구매 버튼 | 결제사 어댑터 구조 + 테스트 결제(샌드박스), 웹훅, 환불, 중복 처리 |
| 관리 화면 | 메뉴만 있는 데모 | 최고 관리자 / 판매자 센터 / 구매자 계정 모두 실제 업무 기능 |

## 2. 역할별 기능

### 최고 관리자 `/admin`
- 대시보드: 오늘·30일 매출, 수수료, 회원, 매출 추이 차트, 처리 대기 업무(상품 심사·입점 신청·환불 요청·납기 지난 제작 주문·문의), 최근 주문, 인기 상품
- 상품: 목록(검색·상태·카테고리·판매자·진열·기간 필터, CSV), 등록/수정, 심사(승인·반려 사유), 판매 중지·보관, 진열·추천, 파일, 변경 이력 / 분류 관리 / 구매평 숨김
- 주문: 전체 주문(필터·CSV), 제작 주문(납기순), 환불 요청, 주문 상세(결제·환불 기록, 유입 경로, 타임라인, 메모, 강제 환불, PG 수동 환불 기록, 영수증 재발송), 결제 내역·웹훅 이벤트
- 회원: 목록(필터·CSV), 상세(주문·보유 콘텐츠·문의·세션), 이용 정지/해제, 비밀번호 재설정 메일, 전체 로그아웃, 콘텐츠 수동 지급/회수 / 운영자 추가·해제
- 판매자: 입점 신청 심사, 판매자 목록·상세, 수수료율 개별 설정, 정지/해제, 메모
- 정산: 판매자별 정산 가능/보류 금액, 정산 배치 생성, 지급 완료(이체 참조번호), 취소, 상세, CSV
- 마케팅: 쿠폰(플랫폼·판매자), 딥링크 성과(클릭·주문·매출·전환율), 메인 배너(이미지 업로드·기간·순서)
- 고객 지원: 1:1 문의 답변, 공지사항, 메일 발송 내역
- 통계: 매출(일별·카테고리별·판매자별), 상품(인기·미판매), 유입 경로(소스·매체·캠페인·딥링크)
- 설정: 사이트·수수료·환불 기간·결제 대기 시간·자동 승인, 결제사 활성화와 연동 상태, 시스템 상태(DB·저장소·메일)
- 로그: 관리 작업 감사 로그, 외부 API/시스템 에러 로그

### 판매자 센터 `/seller`
- 대시보드(매출·순수익·미정산 잔액·차트·처리할 업무·공지), 상품 등록(강의 목차·제작 기간·표지·SEO)·파일 업로드·심사 요청·판매 일시중지
- 주문·제작 대기·환불 요청, 주문 상세에서 제작 시작·납품(메시지+파일)·환불 승인/거절, CSV
- 딥링크(유입 경로·캠페인·언어·쿠폰·만료일, 성과), 판매자 쿠폰, 정산 내역·보류 사유, 고객 문의, 구매평, 스토어 프로필·정산 계좌, 공지

### 구매자
- 쇼핑몰 홈(배너·카테고리·추천·검색/정렬), 상품 상세(강의 목차·구매평·판매자), 판매자 스토어, 공지
- 회원가입·로그인·비밀번호 재설정·이메일 인증, 판매자 입점 신청(`/sell`)
- 결제: 쿠폰 적용, 딥링크 유입 30일 귀속, 결제 수단 선택, 무료 상품 즉시 지급, 결제 대기 주문 재결제/취소
- 내 계정: 라이브러리(파일 다운로드·강의 진도), 주문 상세(영수증·타임라인·환불 요청·납품 파일·구매평), 위시리스트, 1:1 문의, 프로필·비밀번호·로그인 기기 관리

## 3. 검증 기록 (2026-09-17)
- `pnpm typecheck`, `pnpm lint`(경고 0), `pnpm build` 통과
- 링크 크롤러로 관리자 122페이지, 판매자 49, 구매자 41, 비로그인 29페이지 방문 — HTTP 오류·런타임 오류 없음
- `pnpm test:e2e` 전체 흐름 **PGlite와 실제 PostgreSQL 16 양쪽에서 통과**:
  회원가입 → 입점 신청 → 관리자 승인 → 판매자 상품 등록·파일 업로드·심사 요청 → 관리자 승인 → 구매자가 딥링크로 유입·테스트 결제(유입 경로 기록 확인) → 쿠폰 결제(WELCOME10, $20→$18) → 라이브러리 다운로드(비로그인 차단 확인) → 환불 요청 → 판매자 환불 승인 → 다운로드 권한 회수 확인 → 메일 기록 확인
- `pnpm test:security` 로그인 오픈 리다이렉트·시도 제한 우회 회귀 테스트 통과
- `pnpm test:settlement` 정산 환불 15개 항목 통과 (지급 대기 정산서 재계산, 지급 완료 후 환불 차감 생성·다음 정산 합산·정산서 축소 시 차감 복귀)
- 로컬에서 운영 빌드로 확인할 때는 `PAYMENT_TEST_MODE=true`(테스트 결제 허용)와 `COOKIE_SECURE=false`(http 세션 쿠키)가 필요합니다.

## 4. 실서비스 전 반드시 할 일
1. **결제사 연동 (PearPay / NextPay)** — `lib/server/payments/gateway-provider.ts`에 공식 API 명세대로 결제 생성·웹훅 서명 검증·환불 호출을 작성하고 `*_IMPLEMENTED=true`. 현재는 "연동 준비중"으로 표시되고 운영 모드에서는 테스트 결제도 꺼집니다. PG사에 등록할 웹훅 주소는 관리자 → 설정 → 결제 연동에 표시됩니다.
2. **최고 관리자 계정** — 운영 DB에 `RINGO_ADMIN_EMAIL/PASSWORD` 환경변수 또는 `pnpm admin:create`. 예시 계정은 운영에서 생성되지 않습니다.
3. **메일 발송** — Amazon SES SMTP 값 설정(`SMTP_*`, `MAIL_FROM`). 미설정 시 기록만 됩니다.
4. **파일 저장소** — S3 비공개 버킷(`S3_BUCKET`, 버킷 퍼블릭 액세스 차단, 서버 역할에 GetObject/PutObject/DeleteObject 권한).
5. **약관·개인정보 처리방침** — `/terms`, `/privacy`는 자리만 있습니다. 필리핀 Data Privacy Act 기준 문서를 받아 넣으세요.
6. **세금·영수증 정책** — 필리핀 VAT/BIR 영수증, 판매자 원천징수 여부 등 확정 후 반영 필요.
7. **Google / Facebook 로그인** — 추후 진행(버튼은 "연동 예정" 상태).
8. **결제 대기 주문 만료 스케줄** — `POST /api/cron/expire-orders` (헤더 `Authorization: Bearer $CRON_SECRET`)를 EventBridge Scheduler로 10분마다 호출. (스케줄이 없어도 관리자 화면 접속 시 정리됨)

## 5. 알려진 한계 / 다음 개선
- 로그인·업로드 횟수 제한이 서버 인스턴스 메모리 기준 → 여러 대로 확장 시 Redis/ElastiCache로 교체
- 업로드가 서버 메모리를 거침(기본 최대 200MB) → 대용량 강의 영상은 S3 사전 서명 직접 업로드로 전환 권장
- 정산 환불은 자동 처리됩니다: 지급 대기 정산서는 해당 주문을 빼고 금액을 다시 계산(남는 주문이 없으면 취소), 이미 지급된 주문은 음수 "환불 차감" 정산 항목이 생겨 다음 정산서에서 차감됩니다. 차감액이 정산 가능액보다 크면 정산서 생성이 보류됩니다.
- 회원(계정) 정지와 스토어 정지는 별개 — 판매자 계정 정지 시 판매자 관리에서 스토어도 정지
- 상품당 단일 구매(장바구니 없음), 부분 환불 없음(전액 환불)
- 알림·영수증 메일 본문은 아직 영어 (다국어 템플릿은 별도 작업)
- 단일 통화 운영: 상품이나 주문이 생긴 뒤에는 설정에서 통화를 바꿀 수 없음

## 6. AWS 배포 개요
- 권장: **ECS Fargate(또는 App Runner) + RDS PostgreSQL + S3 + SES + CloudFront/ALB(HTTPS)**
- `Dockerfile` 포함 (`output: standalone`, `drizzle/` 마이그레이션 동봉, `/api/health` 헬스체크)
- 필수 환경변수: `TRUSTED_PROXY_HOPS`(ALB 뒤면 1), `APP_URL`, `DATABASE_URL`(+`DATABASE_SSL=require`), `RINGO_ADMIN_EMAIL/PASSWORD`(최초 1회), `S3_BUCKET`, `AWS_REGION`, `SMTP_*`, `MAIL_FROM`, `CRON_SECRET`, 결제사 키
- 서버 시작 시 마이그레이션 자동 적용(`DATABASE_AUTO_MIGRATE=false`로 끄고 배포 파이프라인에서 `pnpm db:migrate` 실행 가능)
- 전체 목록은 `.env.example`
